@model JobsApp.Models.AnnouncementModel

@{
    ViewData["Title"] = "AnnouncementDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Announcement Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Location)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Location)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ExperienceLevel)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ExperienceLevel)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Candidate"))
    {
        @Html.ActionLink("Apply", "ApplyList", "Cv", new {  id=Model.Idannouncement  })
    }
    @if (User.Identity.Name == Model.Idprofile)
    {
        @Html.ActionLink("Applications", "ApplicationsToAnnouncement", "Application", new {  id=Model.Idannouncement  })
    }
    @if (User.Identity.Name == Model.Idprofile)
    {
        @Html.ActionLink("Edit", "Edit", new {  id = Model.Idannouncement  })
    }
    @Html.ActionLink("Company Details", "Details", "Profile", new {  id=Model.Idprofile  })
    <a asp-action="Index">Back to List</a>
</div>
